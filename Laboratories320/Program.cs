using Laboratories320.ПРактика2;
using System;
using System.Threading.Tasks;
using System.Threading;
namespace Laboratories320.Gaifullin
 {
    class Program
    {

        static void Main(string[] args)
        {
            Task task1 = Task.Run(() =>> Continuation.omposition(array));
            Task task2 = task1.ContinueWith(mul => Continuation.Even());
            task22.Wait();
            WarUnit z = new WarUnit
            {
                Speed = 3,
                Armor = 13
                Armor = 13,
                Health = 100
            };
            MongpTest.MongoInsert(z);
            MongpTest.MongoInsert(z).GetAwaiter().GetResult();

        }
    }
}

    






